{
	"name": "06-clock",
	"type": "module",
	"version": "0.4.7",
	"private": true,
	"scripts": {
		"dev": "vite",
		"prebuild": "pnpm run generate",
		"build": "tsc --noEmit && vite build",
		"preview": "vite preview",
		"generate": "yantrix codegen ./src/diagrams/ClockAutomata.mermaid -l TypeScript -o src/generated/ClockAutomata.ts -c ClockAutomata --beautify",
		"lint": "eslint .",
		"format": "eslint --fix ."
	},
	"devDependencies": {
		"@yantrix/core": "workspace:^",
		"typescript": "~5.8.3",
		"vite": "^7.1.7"
	}
}
