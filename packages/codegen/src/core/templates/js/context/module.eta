<%~ include('reducers/module', {
  diagram: it.diagram,
  stateDictionary: it.stateDictionary,
  expressions: it.expressions,
  contextSerializer: it.contextSerializer,
}) %>
<%~ include('defaults/module', {
  stateDictionary: it.stateDictionary,
  diagram: it.diagram,
  expressions: it.expressions,
  startStateKey: it.startStateKey,
  contextSerializer: it.contextSerializer,
}) %>
