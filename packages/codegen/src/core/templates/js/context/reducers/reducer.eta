const reducer = {
<% for (const entry of it.reducer.entries) { %>
  <%~ entry.stateValue %>: (prevContext, payload, functionDictionary, automata) => {
    return <%~ include('../../../shared/context/reducers/contextTransition', { transition: entry.transition }) %>
  },
<% } %>
}
