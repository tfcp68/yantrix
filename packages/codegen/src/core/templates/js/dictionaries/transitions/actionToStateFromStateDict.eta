const actionToStateFromStateDict = {
<%
const stateEntries = Object.entries(it.dictionariesVM.actionToStateFromStateModel ?? {});
for (let index = 0; index < stateEntries.length; index++) {
  const [stateId, actions] = stateEntries[index];
%>
  <%~ include('actionToStateStateEntry', { stateId, actions }) %><% if (index < stateEntries.length - 1) { %>,<% } %>
<% } %>
};


