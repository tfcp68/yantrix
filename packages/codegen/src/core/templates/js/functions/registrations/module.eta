<% for (const registration of it.registrations) { %>
<% if (registration.kind === 'inject') { %>
<%~ include('registerInject', { registration }) %>
<% } else { %>
<%~ include('registerDefine', { registration }) %>
<% } %>
<% } %>


