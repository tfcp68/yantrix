const reducer: TReducerMap = {
<% for (const entry of it.reducer.entries) { %>
  <%~ entry.stateValue %>: (prevContext: TAutomataContext, payload: TReducerPayload, functionDictionary: FunctionDictionary, automata: GenericAutomata) => {
    return <%~ include('../../shared/context/reducers/contextTransition', {
      transition: entry.transition,
    }) %>
  },
<% } %>
}

