eventAdapter.addEventEmitter(
  statesDictionary["<%~ it.emitter.stateId %>"],
  ({ state, context }) => {
    const eventsToEmit = [
      <%~ it.emitter.events.map((eventCode) => include('eventCodeEntry', { eventCode })).join(',\n      ') %>
    ];

    return eventsToEmit[0];
  },
);

