// export type TImports = {
// 	[path: string]: string[];
// };
//
// export function getImports(props: {
// 	imports: TImports;
// }) {
// 	let imports = '';
// 	for (const [key, value] of Object.entries(props.imports)) {
// 		imports += `import { ${value.join(', ')} } from '${key}';\n`;
// 	}
// 	return imports;
// }
